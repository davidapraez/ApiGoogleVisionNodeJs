{
  "name": "procesamientovideo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "dev": "sls offline --stage dev",
    "deploy-dev": "sls deploy --stage dev function -f api",
    "deploy-prod": "sls deploy --stage prod function -f api"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/vision": "^4.0.2",
    "aws-sdk": "^2.1496.0"
  },
  "devDependencies": {
    "serverless-offline": "^13.2.1"
  }
}
